// const request = require('supertest');
// const app = require('../lib/app');

// describe('app routes', () => {
//   it('takes rawRequest and returns an object with body, method, and path', () => {
    
//   });
// });



// if(path === '/') {
//   // GET plain text "hi"
//   console.log('plain text hi');
// } else if(path === '/echo') {
//   // POST status code 200 and plain text with the request body
//   console.log('POST status code 200 and plain text with the request body');
// } else if(path === '/red') {
//   // GET	html with an h1 and the word red
//   console.log('GET html with an h1 and the word red');
// } else if(path === '/green') {
//   // GET	html with an h1 and the word green
//   console.log('GET html with an h1 and the word green');
// } else if(path === '/blue') {
//   // GET	html with an h1 and the word blue
//   console.log('GET html with an h1 and the word blue');
// }